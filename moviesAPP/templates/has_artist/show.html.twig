{% extends 'base.html.twig' %}

{% block title %}HasArtist{% endblock %}

{% block body %}
    <h1>HasArtist</h1>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ has_artist.id }}</td>
            </tr>
            <tr>
                <th>Is_Actor</th>
                <td>{{ has_artist.isActor ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Is_Director</th>
                <td>{{ has_artist.isDirector ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('has_artist_index') }}">back to list</a>

    <a href="{{ path('has_artist_edit', {'id': has_artist.id}) }}">edit</a>

    {{ include('has_artist/_delete_form.html.twig') }}
{% endblock %}
